To start a node, call: gradlew run --args="<port-number>"

Where port-number is a number between 8080 and 8089.

The GUI will show and you can only send a transaction if you login first.

You cannot change the login after logging in.